name: dev1 deploy | dev-branch

on:
  pull_request:
    types:
    - closed

jobs:
  deploy_dev:
    if: contains(github.event.pull_request.labels.*.name, 'cicd') && github.event.pull_request.merged == true
    runs-on: ubuntu-latest
    environment:
      name: development
    # ToDo: Parse event.pull-request body json code block to construct <cluster><app> for argocd
    steps:
      - id: setup
        run: |
          echo Deploying Dev
